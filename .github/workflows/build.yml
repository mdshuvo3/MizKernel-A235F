name: MizKernel-A23-InPlace
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 1. Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf libelf-dev liblz4-tool libncurses-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop rsync squashfs-tools xsltproc zip zlib1g-dev python3 binutils-aarch64-linux-gnu device-tree-compiler
          sudo ln -sf /usr/bin/python3 /usr/bin/python

      - name: 2. Checkout & Toolchain
        run: |
          # 1. Checkout Kernel
          git clone --depth=1 https://github.com/${{ github.repository }} kernel
          
          # 2. Setup Toolchain in root so ../toolchain works
          mkdir -p toolchain/clang-r416183b
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git toolchain/clang-r416183b

      - name: 3. The In-Place Overwrite (Final Fix)
        run: |
          cd $GITHUB_WORKSPACE
          # 1. Clone KernelSU to a neutral temp folder
          git clone --depth=1 https://github.com/rifsxd/KernelSU-Next ksu_temp
          
          # 2. FORCE CLEAN the destination (bypass any file-exists or symlink issues)
          rm -rf kernel/drivers/kernelsu
          mkdir -p kernel/drivers/kernelsu
          
          # 3. RSYNC the files into place (more reliable than cp for deep directory trees)
          rsync -av ksu_temp/ kernel/drivers/kernelsu/
          
          # 4. RESET drivers/Kconfig (Undo all our previous 'sed' experiments to match original source)
          # Your log shows it wants: "drivers/kernelsu/Kconfig"
          cd kernel/drivers
          echo 'source "drivers/kernelsu/Kconfig"' > Kconfig.tmp
          tail -n +2 Kconfig >> Kconfig.tmp # Keep everything after line 1
          # Note: If line 4 was the original, we ensure the file matches that expectation.
          # Let's just restore the file to its original state if you committed our changes.
          git checkout Kconfig || true 

      - name: 4. The Build
        run: |
          cd $GITHUB_WORKSPACE/kernel
          
          TC_PATH=$GITHUB_WORKSPACE/toolchain/clang-r416183b/bin
          
          BUILD_ARGS="ARCH=arm64 \
                      CC=$TC_PATH/clang \
                      LD=$TC_PATH/ld.lld \
                      AR=$TC_PATH/llvm-ar \
                      NM=$TC_PATH/llvm-nm \
                      OBJCOPY=$TC_PATH/llvm-objcopy \
                      OBJDUMP=$TC_PATH/llvm-objdump \
                      STRIP=$TC_PATH/llvm-strip \
                      CROSS_COMPILE=aarch64-linux-gnu- \
                      CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
                      LLVM=1 \
                      LLVM_IAS=0"

          # Step A: Defconfig
          make $BUILD_ARGS vendor/a23_eur_open_mizkernel_defconfig
          
          # Step B: Compile
          make -j$(nproc) $BUILD_ARGS

      - name: 5. Package
        run: |
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AnyKernel3
          cd AnyKernel3
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
          sed -i 's!block=.*!block=auto;!g' anykernel.sh
          cp ../kernel/arch/arm64/boot/Image* . || true
          zip -r9 ../MizKernel-A23.zip * -x .git README.md

      - name: 6. Upload
        uses: actions/upload-artifact@v4
        with:
          name: MizKernel-A23
          path: MizKernel-A23.zip
